= form_for [@commentable, @comment] do |f|
  .row
    .col-md-12
      - if @comment.errors.any?
        .alert.bg-danger.alert-styled-left
          %button.close{ data: { dismiss: 'alert' } }
            %span &times;
            %span.sr-only Закрыть
          %h4
            Исправьте ошибки и попробуйте снова. Ошибок: 
            = @comment.errors.count
          %ul
            - @comment.errors.full_messages.each do |msg|
              %li= msg
      .form-group
        .input-group
          %span.input-group-addon
            .icon-pencil
          = f.text_area :body, class: 'form-control', placeholder: 'Добавить комментарий'
          = f.hidden_field :user_id, value: current_user.id
    .col-md-12
      .text-left
        = f.submit 'Добавить комментарий', class: 'btn btn-info'